{% extends 'base-front.html.twig' %}

{% block title %}Register{% endblock %}

{% block Content %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}



    <head>
        <meta charset="utf-8">
        <title>Login and Registration Form in HTML | CodingNepal</title>
        <link rel="stylesheet" href="{{ asset('base-front/stylei.css') }}">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
    <div class="wrapper">
        <!--div class="title-text">
           <div class="title login">
              Annonceur
           </div>
           <div class="title signup">
              Client
           </div>
       ** </div-->
        <center class="form-container">
            <div class="slide-controls">
                <input type="radio" name="slide" id="login" checked>
                <input type="radio" name="slide" id="signup">
                <label for="login" class="slide login">Annonceur</label>
                <label for="signup" class="slide signup">Client</label>
                <div class="slider-tab"></div>
            </div>



            <div class="form-inner">
                <form method="post" class="login">
                    {{ form_start(registrationForm) }}
                    <div class="field">
                        {{ form_row(registrationForm.email) }}
                    </div>
                    <div class="field">
                        {{ form_row(registrationForm.plainPassword, {
                            label: 'Password'
                        }) }}
                    </div>
                    <div class="field">
                        {{ form_row(registrationForm.FirstName) }}
                    </div>
                    <div class="field">
                        {{ form_row(registrationForm.LastName) }}
                    </div>
                    <div class="field">
                        <br/>
                        <label><h3> Catégorie :  </h3></label>
                    </div>
                    <div>
                        <center>
                            <ul>
                                <h3> <li><input type="checkbox" id="carte" name="carte" checked>
                                        <label for="carte">Carte d'invitation</label></li>

                                    <li>  <input type="checkbox" id="centre " name="centre">
                                        <label for="centre">Centre de beauté</label></li>
                                    <li>  <input type="checkbox" id="centre " name="centre">
                                        <label for="centre">Salles des fétes</label></li></h3>



                            </ul>
                        </center>
                    </div>




                    <div class="field btn">
                        <div class="btn-layer"></div>
                        <input type="submit" value="OK">
                    </div>Client
                    {{ form_row(registrationForm.agreeTerms) }}


                    <!--div class="signup-link">
                       Not a member? <a href="">Signup now</a>
                    </div-->
                </form>
                <form method="post" class="login">

                    <div class="field">
                        {{ form_row(registrationForm.email) }}
                    </div>
                    <div class="field">
                        {{ form_row(registrationForm.plainPassword, {
                            label: 'Password'
                        }) }}
                    </div>
                    <div class="field">
                        {{ form_row(registrationForm.FirstName) }}
                    </div>
                    <div class="field">
                        {{ form_row(registrationForm.LastName) }}
                    </div>
                    <div class="field">
                        <br/>
                        <label><h3> Catégorie :  </h3></label>
                    </div>
                    <div>
                        <center>
                            <ul>
                                <h3> <li><input type="checkbox" id="carte" name="carte" checked>
                                        <label for="carte">Carte d'invitation</label></li>

                                    <li>  <input type="checkbox" id="centre " name="centre">
                                        <label for="centre">Centre de beauté</label></li>
                                    <li>  <input type="checkbox" id="centre " name="centre">
                                        <label for="centre">Salles des fétes</label></li></h3>



                            </ul>
                        </center>
                    </div>




                    <div class="field btn">
                        <div class="btn-layer"></div>
                        <input type="submit" value="OK">
                    </div>
                    {{ form_row(registrationForm.agreeTerms) }}
                    {{ form_end(registrationForm) }}

                    <!--div class="signup-link">
                       Not a member? <a href="">Signup now</a>
                    </div-->
                </form>
            </div>
        </center>
                    </center>
        </div>
    </div>
    <script>
        const loginText = document.querySelector(".title-text .login");
        const loginForm = document.querySelector("form.login");
        const loginBtn = document.querySelector("label.login");
        const signupBtn = document.querySelector("label.signup");
        const signupLink = document.querySelector("form .signup-link a");
        signupBtn.onclick = (()=>{
            loginForm.style.marginLeft = "-50%";
            loginText.style.marginLeft = "-50%";
        });
        loginBtn.onclick = (()=>{
            loginForm.style.marginLeft = "0%";
            loginText.style.marginLeft = "0%";
        });
        signupLink.onclick = (()=>{
            signupBtn.click();
            return false;
        });
    </script>
    </body>

{% endblock %}

